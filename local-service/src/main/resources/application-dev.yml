spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
      data-locations: classpath*:db/devdata/data.sql

  liquibase:
    enabled: false

logging:
  level:
    root: info